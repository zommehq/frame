{
  "name": "micro-fe",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun --filter '@micro-fe/fragment-elements' run build && bun --filter './apps/*' run build",
    "dev": "bun --filter './apps/*' run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
